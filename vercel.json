{
  "rewrites": [
    { "source": "/example/:path*", "destination": "/example/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "buildCommand": "npm run build && cp -r example-code dist/example",
  "outputDirectory": "dist"
}
